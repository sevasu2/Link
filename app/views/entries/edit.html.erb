<% @page_title = "ポートフォリオ記事の編集" %>
<h1><%= @page_title %></h1>

<div class="toolbar admin-regi"><%= link_to "記事の表示に戻る", @entry %></div>

<%= form_for @entry do |form| %>
  <%= render "form", form: form %>
  <div class="event-submit"><%= form.submit %></div>
<% end %>
